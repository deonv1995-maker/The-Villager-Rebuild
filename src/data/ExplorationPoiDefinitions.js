// Authored exploration POIs intentionally start empty.
//
// The former northern cave was removed when excavation became a world-wide
// tunneling system. Underground pockets are generated deterministically by
// UndergroundTunnelingSystem and are not prebuilt surface entrances.
export const EXPLORATION_POIS = Object.freeze([]);
