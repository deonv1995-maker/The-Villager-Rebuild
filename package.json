{
  "name": "the-villager-rebuild",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "verify:assets": "node scripts/verify-runtime-assets.mjs public",
    "verify:gameplay": "node scripts/verify-gameplay.mjs",
    "verify:dist-assets": "node scripts/verify-runtime-assets.mjs dist",
    "check": "npm run verify:assets && npm run verify:gameplay && npm run build && npm run verify:dist-assets"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "7.1.3"
  }
}
