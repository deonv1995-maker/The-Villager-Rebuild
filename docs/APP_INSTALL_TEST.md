Android acceptance for the app shell: open the deployed Pages build in full Chrome, confirm it loads Foundation 0.3.2 without a stale build, confirm Chrome exposes install rather than only a plain shortcut when installability criteria are met, install The Villager, verify the archived Villager icon is used, and verify launch is fullscreen with landscape preference. Do not resume gameplay feature work until this shell is visually accepted on-device.
