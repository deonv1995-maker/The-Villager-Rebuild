Android recovery acceptance for the app shell: open the deployed Pages build in full Chrome, let it remain open for at least 30 seconds and interact with the page once, then open Chrome's menu. The recovery baseline passes only if Chrome exposes the native Install app option rather than only Add to Home screen/Create shortcut. For this baseline test, the original PR #16 Villager icon is expected. Do not evaluate the Ranger replacement icon until native installation is restored. Do not resume gameplay or water work until this baseline is confirmed on-device.
