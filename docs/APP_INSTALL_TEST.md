Android acceptance for the app shell: open the deployed Pages build in full Chrome, confirm it loads Foundation 0.3.2 without a stale build, confirm Chrome exposes the real install option rather than only Create shortcut, install The Villager, verify the low-poly Ranger/villager launcher icon is shown instead of a generic `G`, and verify launch is fullscreen with landscape preference. Any Create shortcut-only result or generic browser icon is an app-shell failure. Do not resume gameplay feature work until this shell is visually accepted on-device.
