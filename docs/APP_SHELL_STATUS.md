Status: Chrome-native install flow was proven on Android at PR #16. PR #18 regressed that working contract by adding query-string cache revisions to manifest icon `src` values; on-device Chrome again showed Create shortcut + generic G. `0.3.2-install6` restores the PR #16 clean relative manifest-icon pattern and assigns the Ranger artwork new physical PNG filenames so the icon changes without altering the native install architecture. Pending CI, Pages deployment and on-device install acceptance before gameplay work resumes.
