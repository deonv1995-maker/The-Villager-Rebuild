Status: Android Chrome native installation is working and the launcher-icon pass is accepted on-device. Chrome owns the real **Install app** flow. The repository still has GitHub's dynamic branch-source `pages build and deployment` plus the custom Vite `dist` deployment, but the production workflow waits for the dynamic Pages run for the same commit SHA and deploys `dist` last. That guarantees `manifest.webmanifest`, `sw.js`, and launcher icons finish at the deployed site root where Chrome expects them.

The proven manifest identity/scope/display configuration, simple no-store service worker, Chrome-owned install flow, and deterministic Pages ordering are now a locked shell foundation while gameplay development resumes. The launcher artwork uses the approved Ranger portrait based closely on the actual in-game character: brown hair, moustache/goatee, blue scarf, cream tunic, brown equipment, and the low-poly forest setting. The deterministic build step reconstructs the approved source and writes truecolor 192x192, 512x512, and maskable 512x512 PNGs into the production `dist/icons` canonical paths after Vite builds. CI verifies dimensions, decoded-pixel hashes, canonical/reference parity, and that no custom install interception returns.

Android verification identified and resolved the final icon conflict. Chrome History rendered the approved Ranger PNG correctly while the native PWA install sheet showed the older blue-background/orange-haired Ranger artwork. That artwork matched `public/icons/icon.svg`, which had still been advertised by the manifest as an `image/svg+xml` icon with `sizes: any`. The manifest now exposes only the three approved PNG launcher candidates: 192x192 `any`, 512x512 `any`, and 512x512 `maskable`. The legacy SVG files may remain in the repository as non-install assets, but they are not manifest candidates. CI fails if an SVG or `sizes: any` launcher candidate is reintroduced. The working shell remains on manifest revision `ranger-icon-5`.

Gameplay milestones must preserve this architecture. PWA changes should be made only for an explicit install/deployment requirement or a demonstrated regression, and must retain the branch-source-first / production-dist-last deployment gate.
