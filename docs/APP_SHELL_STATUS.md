Status: Android Chrome native installation is working again after the Pages deployment-order fix. The user has verified the real Chrome **Install app** flow on-device. The repository still has GitHub's dynamic branch-source `pages build and deployment` plus the custom Vite `dist` deployment, but the production workflow waits for the dynamic Pages run for the same commit SHA and deploys `dist` last. That guarantees `manifest.webmanifest`, `sw.js`, and launcher icons finish at the deployed site root where Chrome expects them.

The current pass remains icon-only. The proven manifest identity/scope/display configuration, simple no-store service worker, Chrome-owned install flow, and deterministic Pages ordering are unchanged. The installed icon artwork is being replaced with a new 1:1 scene generated from the user's current in-game screenshot. The scene is resized into truecolor 192x192, 512x512, and full-bleed 512x512 maskable PNGs at the existing canonical paths; the existing vector Ranger SVGs remain fallback-only. The browser favicon/touch icon now points at the same canonical 192x192 scene PNG. CI verifies dimensions, truecolor PNG encoding, fixed artwork hashes, canonical/reference parity, and that no custom install interception returns.

Pending: green CI, merge, successful branch-source Pages deployment followed by the later production `dist` deployment for the same commit, then Android device verification that reinstalling through Chrome still uses the native **Install app** flow and shows the new screenshot-derived launcher icon.
