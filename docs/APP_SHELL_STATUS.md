Status: Android Chrome's native Install option was proven on-device at PR #16 (`64a4ee7eb85da5f912c014855b6bc39bf661375f`), but later icon work regressed Chrome to Create shortcut + generic G. The canonical 192x192, 512x512 and SVG icon files currently match PR #16 byte-for-byte, so icon corruption is ruled out. Investigation also showed that the rebuild added a replayable shell cache that did not exist in the archived original game; this cache allowed old HTML/install state to survive later successful deployments. The current scoped recovery removes that shell cache, restores the archived network-fresh service-worker behavior for same-origin files, bumps the shell revision to `0.3.2-install8`, preserves the PR #16 manifest/icon bytes, and keeps the user-requested Install App diagnostic control visible by default. Ranger replacement artwork remains unused. Pending CI, Pages deployment, and on-device verification that the visible Install App control reaches Chrome's native installer or reports that Chrome has not emitted `beforeinstallprompt`.
