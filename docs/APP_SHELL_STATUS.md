Status: Android Chrome installation is currently blocked by a GitHub Pages deployment-order conflict, not by the current manifest contents. The repository has two Pages pipelines attached to `main`: GitHub's dynamic branch-source `pages build and deployment` artifact and the custom Vite `dist` deployment. The `dist` artifact contains `manifest.webmanifest`, `sw.js`, and launcher icons at the deployed root, but the branch-source artifact leaves those files below `public/` while root `index.html` requests them from the site root. The branch-source deployment has been finishing after the correct `dist` deployment, allowing the game to remain playable in direct-static mode while Chrome cannot load the root PWA install resources and falls back to **Create shortcut**. The recovery changes deployment ordering so the production `dist` artifact always deploys after the dynamic Pages build. The current manifest retains explicit 192x192, 512x512, maskable PNG, and SVG launcher declarations; custom install UI, `beforeinstallprompt` interception, and the Android wrapper remain removed. Pending: green CI, merge, proof that the final Pages deployment is the `dist` artifact, then one Android Chrome verification of the native **Install app** flow.
