Status: the remaining concrete installability blocker was identified after restoring the archived first-game shell. The archived manifest declares only SVG icons, but current Chromium installability guidance requires explicit 192x192 and 512x512 raster icons; SVG icons are supported but should have raster fallbacks. The current recovery keeps the archived identity/display/service-worker architecture, adds the known-good Villager 192/512/maskable PNG declarations alongside the archived SVG pair, and bumps the versioned manifest/service-worker URL so Chrome must re-read the install shell. Custom install UI, `beforeinstallprompt` interception, and the Android wrapper remain removed. Pending: green CI, successful Pages deployment, then Android Chrome verification that the native **Install app** flow returns.
