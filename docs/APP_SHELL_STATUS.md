Status: Android Chrome native installation is working again after the Pages deployment-order fix. The repository still has GitHub's dynamic branch-source `pages build and deployment` plus the custom Vite `dist` deployment, but the production workflow now waits for the dynamic Pages run for the same commit SHA and deploys `dist` last. That guarantees `manifest.webmanifest`, `sw.js`, and launcher icons finish at the deployed site root where Chrome expects them.

The current pass is icon-only. The proven manifest identity/scope/display configuration, simple no-store service worker, Chrome-owned install flow, and deterministic Pages ordering are unchanged. The existing generated low-poly Ranger PNG artwork is promoted into the canonical `icons/icon-192.png`, `icons/icon-512.png`, and `icons/icon-maskable-512.png` paths. The SVG fallbacks are updated to matching vector Ranger artwork, and the shell query revision is bumped only to force browsers to fetch the new icon assets. CI verifies that the canonical PNGs remain byte-identical to the generated Ranger source files and that no custom install interception returns.

Pending: green CI, merge, successful branch-source Pages deployment followed by the later production `dist` deployment for the same commit, then Android device verification that reinstalling through Chrome still uses the native **Install app** flow and shows the Ranger launcher icon.
