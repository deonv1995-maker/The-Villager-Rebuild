Status: Android Chrome native installation is working again after the Pages deployment-order fix. The user has verified the real Chrome **Install app** flow on-device. The repository still has GitHub's dynamic branch-source `pages build and deployment` plus the custom Vite `dist` deployment, but the production workflow waits for the dynamic Pages run for the same commit SHA and deploys `dist` last. That guarantees `manifest.webmanifest`, `sw.js`, and launcher icons finish at the deployed site root where Chrome expects them.

The current pass remains icon-only. The proven manifest identity/scope/display configuration, simple no-store service worker, Chrome-owned install flow, and deterministic Pages ordering are unchanged. The launcher artwork uses the approved Ranger portrait based closely on the actual in-game character: brown hair, moustache/goatee, blue scarf, cream tunic, brown equipment, and the low-poly forest setting. The deterministic build step reconstructs the approved source and writes truecolor 192x192, 512x512, and maskable 512x512 PNGs into the production `dist/icons` canonical paths after Vite builds. CI verifies dimensions, decoded-pixel hashes, canonical/reference parity, and that no custom install interception returns.

Android verification showed that Chrome History was already rendering the new Ranger image while the PWA install sheet still showed the older launcher artwork. That isolates the remaining problem to Chrome's install/launcher metadata cache rather than the deployed image bytes. Query-string revisions were not sufficient for that metadata path, so the manifest now points at fresh physical PNG resource names (`ranger-install-192-v4.png`, `ranger-install-512-v4.png`, and `ranger-install-maskable-512-v4.png`). The build creates those files byte-identically from the approved canonical Ranger outputs, and CI enforces that parity. This changes only launcher resource identity; manifest `id`, scope, display configuration, service-worker behavior, native Chrome installation, and deployment ordering remain unchanged.

Pending: green CI, merge, successful branch-source Pages deployment followed by the later production `dist` deployment for the same commit, then Android device verification that the Chrome install sheet and installed launcher both show the approved Ranger icon.
