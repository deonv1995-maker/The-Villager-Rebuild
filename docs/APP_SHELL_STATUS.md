Status: Android Chrome's native Install option was proven on-device at PR #16 (`64a4ee7eb85da5f912c014855b6bc39bf661375f`), but later icon work regressed Chrome to Create shortcut + generic G. The exact PR #16 shell was restored and still produced Create shortcut on-device even after clearing Chrome site data. The current scoped pass keeps the proven manifest/service-worker/icon baseline and adds a user-requested in-game Install App button. The button only triggers Chrome's native installer when `beforeinstallprompt` is actually emitted; otherwise it reports that native installation is unavailable rather than pretending to force an install. Ranger replacement artwork remains unused until installation itself is proven again. Pending CI, Pages deployment, and on-device verification of the new install affordance.
