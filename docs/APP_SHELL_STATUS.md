Status: the installation layer is being restored to the archived first game's working PWA contract rather than another approximation. The rebuild now uses the archived manifest structure (`display_override`, `orientation: any`, and the two SVG launcher declarations), the archived network-fresh service-worker behavior that deletes all Cache Storage entries, and the archived simple versioned manifest/service-worker registration pattern. Custom install UI, `beforeinstallprompt` interception, and the temporary Android wrapper remain removed. Ranger PNG artwork is intentionally inactive until Chrome native installation is proven again. Pending: green CI, successful Pages deployment, then Android Chrome verification that the native **Install app** flow returns.
