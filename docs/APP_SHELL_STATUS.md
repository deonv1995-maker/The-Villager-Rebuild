Status: installation is being reset to a single Chrome-owned PWA/WebAPK path. The previous custom in-game installer captured `beforeinstallprompt` and called `preventDefault()`, which made our page compete with Chrome for install UI ownership. The temporary Android test wrapper is also being removed so there is no second native packaging path. The new shell uses one manifest, one service worker, and the Ranger 192/512/maskable PNG launcher set. CI verifies that no custom install interception or legacy install UI is emitted. Pending: green CI, successful GitHub Pages deployment, then a clean Android Chrome device test proving that the browser exposes **Install app** rather than only **Create shortcut**.
