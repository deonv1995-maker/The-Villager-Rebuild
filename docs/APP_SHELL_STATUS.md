Status: Android Chrome installation is blocked by a GitHub Pages deployment-order conflict, not by the current manifest contents. The repository has two Pages pipelines attached to `main`: GitHub's dynamic branch-source `pages build and deployment` artifact and the custom Vite `dist` deployment. The `dist` artifact contains `manifest.webmanifest`, `sw.js`, and launcher icons at the deployed root, but the branch-source artifact leaves those files below `public/` while root `index.html` requests them from the site root. The branch-source deployment has repeatedly finished after the correct `dist` deployment, allowing the game to remain playable in direct-static mode while Chrome cannot load the final root PWA install resources and falls back to **Create shortcut**.

An attempted recovery using `workflow_run` on GitHub's internal dynamic Pages workflow did not trigger and is not considered a fix. The current recovery instead runs the production deploy on each `main` push, builds/checks `dist`, polls the Actions API for the dynamic Pages run with the exact same commit SHA, waits for it to complete successfully, and only then uploads/deploys `dist`. This guarantees the production PWA artifact is final without relying on race timing. The manifest retains explicit 192x192, 512x512, maskable PNG, and SVG launcher declarations; custom install UI, `beforeinstallprompt` interception, and the Android wrapper remain removed.

Pending: green CI, merge, successful branch-source Pages deployment followed by a later successful production `dist` deployment for the same commit, then one Android Chrome verification of the native **Install app** flow.
